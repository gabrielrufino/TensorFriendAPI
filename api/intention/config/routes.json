{
  "routes": [
    {
      "method": "GET",
      "path": "/intentions",
      "handler": "intention.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/intentions/count",
      "handler": "intention.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/intentions/:id",
      "handler": "intention.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/intentions",
      "handler": "intention.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/intentions/:id",
      "handler": "intention.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/intentions/:id",
      "handler": "intention.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
